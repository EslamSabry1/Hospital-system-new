line-length = 120
target-version = "py311"

[lint]
select = ["E", "W", "F", "I"]
ignore = [
  "E501",   # line too long (handled by formatter)
  "F401",   # unused imports (some are re-exports)
]

[lint.per-file-ignores]
"devices/migrations/*.py" = ["E", "W", "F"]
"reset_db.py" = ["F"]
